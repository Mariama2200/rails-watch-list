<div class="container py-4">
  <h1>New list</h1>

  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%= f.submit "Create", class: "btn btn-primary" %>
  <% end %>

  <%= link_to "Back", lists_path, class: "btn btn-outline-secondary mt-3" %>
</div>
